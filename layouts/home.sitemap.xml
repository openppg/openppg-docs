{{- /* Site-wide sitemap.xml.

  The theme's default sitemap template ranges over `.Pages`, which on the home page
  is only the top-level sections — that produced a sitemap containing a single URL.
  This template walks every page in the site instead, skipping taxonomy and term
  pages (unused here) and anything opting out via `sitemap_exclude` front matter.
*/ -}}
{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {{- range site.Pages }}
    {{- if and (in (slice "home" "section" "page") .Kind) (ne .Params.sitemap_exclude true) (ne .Sitemap.Disable true) }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ safeHTML ( .Lastmod.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>
    {{- end }}
    {{- with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>
    {{- end }}
    {{- if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>
    {{- end }}
  </url>
    {{- end }}
  {{- end }}
</urlset>
